<?xml version="1.0" encoding="utf-8"?>
<!-- Trends range chip, selected (flewtube family). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/kuber_flewtube_primary_subtle" />
    <stroke android:width="1dp" android:color="@color/kuber_flewtube_primary" />
    <corners android:radius="8dp" />
</shape>
